import React from 'react';
import { style } from './TagStyle';

const Tag = (props) => {
  return (
    <>
      <Tagitem>tag</Tagitem>
    </>
  );
};

export default Tag;

const { Tagitem } = style;
